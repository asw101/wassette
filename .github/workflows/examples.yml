name: Publish Examples

on:
  push:
    branches: [ "main" ]
    paths:
      - 'examples/**'
      - '.github/workflows/examples.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'examples/**'
      - '.github/workflows/examples.yml'
  workflow_dispatch:
    inputs:
      tag:
        description: 'The tag to use for the OCI artifacts (e.g., v0.4.0 or latest).'
        required: true
        type: string
        default: 'latest'

env:
  CARGO_TERM_COLOR: always
  REGISTRY: ghcr.io
  GO_VERSION: 1.26.5
  TINYGO_VERSION: 0.41.1

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        ref: ${{ startsWith(github.event.inputs.tag, 'v') && github.event.inputs.tag || github.ref }}
    - uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3 # v4.0.0
    - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
    - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
      with:
        go-version: ${{ env.GO_VERSION }}
    - uses: acifani/setup-tinygo@dd8a7075d951a7595b2ef2123ed0ab1af0c13e56 # v3.0.0
      with:
        tinygo-version: ${{ env.TINYGO_VERSION }}
    - uses: bytecodealliance/actions/wasm-tools/setup@9152e710e9f7182e4c29ad218e4f335a7b203613 # v1.1.3
    - run: ./scripts/install-protobuf.sh
      shell: bash
    - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
    - run: rustup target add wasm32-wasip2

    - name: Install wit-docs-inject
      run: cargo install --git https://github.com/Mossaka/wit-docs-inject

    - name: Build
      run: just build-examples

    - name: Upload wasm
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: examples
        path: bin

  publish:
    runs-on: ubuntu-latest
    if: |
      github.event_name != 'pull_request' &&
      (github.event_name != 'workflow_dispatch' || !contains(github.event.inputs.tag, '-'))
    permissions:
      contents: read
      packages: write
      id-token: write
    needs: build
    strategy:
      matrix:
        component:
          - name: arxiv-rs
            file: arxiv-rs.wasm
            description: An arXiv research component for searching and downloading papers written in Rust
          - name: github-js
            file: github-js.wasm
            description: A comprehensive GitHub REST API component written in JavaScript
          - name: gomodule-go
            file: gomodule.wasm
            description: A Go module component
          - name: eval-py
            file: eval-py.wasm
            description: A Python expression evaluation component
          - name: time-server-js
            file: time-server-js.wasm
            description: A time server component written in JavaScript
          - name: get-weather-js
            file: get-weather-js.wasm
            description: A weather component written in JavaScript
          - name: memory-js
            file: memory-js.wasm
            description: A knowledge graph memory storage component written in JavaScript
          - name: filesystem-rs
            file: filesystem.wasm
            description: A filesystem component written in Rust
          - name: fetch-rs
            file: fetch-rs.wasm
            description: A fetch component written in Rust
          - name: brave-search-rs
            file: brave-search-rs.wasm
            description: A web search component using Brave Search API written in Rust
          - name: get-open-meteo-weather-js
            file: get-open-meteo-weather-js.wasm
            description: A weather component using Open-Meteo API written in JavaScript
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: examples
          path: bin

      - name: Log in to Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: 'v2.4.1'
      - name: Install wkg
        run: |
          curl -L --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/cargo-bins/cargo-binstall/main/install-from-binstall-release.sh |
            bash
          cargo binstall --no-confirm wkg@0.15.1

      - name: Determine tag
        id: meta
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "latest=true" >> $GITHUB_OUTPUT
            if [[ "$INPUT_TAG" == v* ]]; then
              echo "tag=${INPUT_TAG#v}" >> $GITHUB_OUTPUT
              echo "release_tag=latest" >> $GITHUB_OUTPUT
            else
              echo "tag=$GITHUB_SHA" >> $GITHUB_OUTPUT
              echo "release_tag=$INPUT_TAG" >> $GITHUB_OUTPUT
            fi
          else
            echo "tag=$GITHUB_SHA" >> $GITHUB_OUTPUT
            echo "latest=false" >> $GITHUB_OUTPUT
          fi
      - name: Publish ${{ matrix.component.name }}
        id: publish_versioned
        env:
          FILE: bin/${{ matrix.component.file }}
          OCI_REFERENCE: ghcr.io/${{ github.repository_owner }}/wassette/${{ matrix.component.name }}
          LEGACY_OCI_REFERENCE: ghcr.io/${{ github.repository_owner }}/${{ matrix.component.name }}
          TITLE: ${{ matrix.component.name }}
          DESCRIPTION: ${{ matrix.component.description }}
          VERSION: ${{ steps.meta.outputs.tag }}
        run: |
          set -euo pipefail
          repo_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"
          push() {
            wkg oci push "$1:$VERSION" "$FILE" \
              --annotation "org.opencontainers.image.title=${TITLE}" \
              --annotation "org.opencontainers.image.description=${DESCRIPTION}" \
              --annotation "org.opencontainers.image.source=${repo_url}" \
              --annotation "org.opencontainers.image.url=${repo_url}" \
              --annotation "org.opencontainers.image.version=$VERSION" \
              --annotation "org.opencontainers.image.revision=${GITHUB_SHA}" \
              --annotation "org.opencontainers.image.licenses=MIT"
          }
          output=$(push "$OCI_REFERENCE")
          echo "$output"
          digest=$(echo "$output" | grep -oP 'digest: \K.*')
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          # Transitional: keep the unprefixed reference publishing so existing
          # oci://ghcr.io/microsoft/<name> URIs keep resolving. Drop this once
          # consumers have moved to the wassette/ namespace.
          legacy_output=$(push "$LEGACY_OCI_REFERENCE")
          echo "$legacy_output"
          echo "legacy_digest=$(echo "$legacy_output" | grep -oP 'digest: \K.*')" >> "$GITHUB_OUTPUT"
      - name: Sign container image ${{ matrix.component.name }}:${{ steps.meta.outputs.tag }}
        run: |
          cosign sign --yes ghcr.io/${{ github.repository_owner }}/wassette/${{ matrix.component.name }}@${{ steps.publish_versioned.outputs.digest }}
          cosign sign --yes ghcr.io/${{ github.repository_owner }}/${{ matrix.component.name }}@${{ steps.publish_versioned.outputs.legacy_digest }}

      - name: Publish ${{ matrix.component.name }} with tag ${{ steps.meta.outputs.release_tag || 'latest' }} (if specified)
        id: publish_latest
        if: steps.meta.outputs.latest == 'true'
        env:
          FILE: bin/${{ matrix.component.file }}
          OCI_REFERENCE: ghcr.io/${{ github.repository_owner }}/wassette/${{ matrix.component.name }}
          LEGACY_OCI_REFERENCE: ghcr.io/${{ github.repository_owner }}/${{ matrix.component.name }}
          TITLE: ${{ matrix.component.name }}
          DESCRIPTION: ${{ matrix.component.description }}
          VERSION: ${{ steps.meta.outputs.release_tag || 'latest' }}
        run: |
          set -euo pipefail
          repo_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"
          push() {
            wkg oci push "$1:$VERSION" "$FILE" \
              --annotation "org.opencontainers.image.title=${TITLE}" \
              --annotation "org.opencontainers.image.description=${DESCRIPTION}" \
              --annotation "org.opencontainers.image.source=${repo_url}" \
              --annotation "org.opencontainers.image.url=${repo_url}" \
              --annotation "org.opencontainers.image.version=$VERSION" \
              --annotation "org.opencontainers.image.revision=${GITHUB_SHA}" \
              --annotation "org.opencontainers.image.licenses=MIT"
          }
          output=$(push "$OCI_REFERENCE")
          echo "$output"
          digest=$(echo "$output" | grep -oP 'digest: \K.*')
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          # Transitional — see the note on the versioned push above.
          legacy_output=$(push "$LEGACY_OCI_REFERENCE")
          echo "$legacy_output"
          echo "legacy_digest=$(echo "$legacy_output" | grep -oP 'digest: \K.*')" >> "$GITHUB_OUTPUT"
      - name: Sign container image ${{ matrix.component.name }}:${{ steps.meta.outputs.release_tag || 'latest' }}
        if: steps.meta.outputs.latest == 'true'
        run: |
          cosign sign --yes ghcr.io/${{ github.repository_owner }}/wassette/${{ matrix.component.name }}@${{ steps.publish_latest.outputs.digest }}
          cosign sign --yes ghcr.io/${{ github.repository_owner }}/${{ matrix.component.name }}@${{ steps.publish_latest.outputs.legacy_digest }}
